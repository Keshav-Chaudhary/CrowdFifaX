// Test stub for the `server-only` package. The real package throws if imported
// into a client bundle; in unit tests we only exercise pure logic, so this is a
// no-op. The runtime guard remains active in the actual Next.js build.
export {};
